# Windows ping

Small toast, bottom right. **Does not steal focus.** Silent by default. `-Sound` if you want a ding.

```
powershell -NoProfile -ExecutionPolicy Bypass -File toast.ps1 -Title "Done" -Body "Kindle is installed." -Sound
```

Mute: empty file `MUTE` next to the script.
