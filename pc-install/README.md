# PC install

You name a program. Winget installs it. You do not open Windows Settings → Apps.

```
winget search kindle
winget install --id Amazon.Kindle --exact --silent --accept-package-agreements --accept-source-agreements
```
